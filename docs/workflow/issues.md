# Issues

Use the issue form to create work items. New issues start with the `status:todo` label by default.

In `Details`, describe:

- problem or goal
- expected outcome
- acceptance criteria (checkboxes)
- context or notes

When starting implementation, create and use an issue-specific branch (for example: `issue/<id>-short-name`).