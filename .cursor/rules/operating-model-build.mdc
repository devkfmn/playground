---
description: Plan Build and post-plan implementation must delegate to coding-clanker; never inline feature implementation.
alwaysApply: true
---

# Build and implementation delegation

- When the user runs **Build** on an **accepted plan**, or when you would otherwise implement planned feature work in the workspace, you **must not** edit application or feature code yourself.
- Instead, use the **Task** tool to delegate **`coding-clanker`** (subagent type `coding-clanker`). The delegated task text **must** include the GitHub issue reference **`#<n>`** so hooks can set **`status:in-progress`** / **`status:in-review`** on the issue.
- Pass the accepted plan (path or summary), suggested branch name, and scope in the delegation prompt.
- Details: [.cursor/rules/git-workflow.mdc](.cursor/rules/git-workflow.mdc), [.cursor/agents/coding-clanker.md](.cursor/agents/coding-clanker.md).
